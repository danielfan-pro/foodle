<!DOCTYPE html>
<html>
<head>
  <title>Foodle</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= csrf_meta_tags %>
  <%= render 'application/favicon' %>
  <script src="https://kit.fontawesome.com/93f0a04255.js" crossorigin="anonymous"></script>
</head>
<body>
  
  <nav class="top-bar">
    <a href="/restaurants"><%= image_tag("foodle logo long.png", alt: "logo", :class => "logo-nav-bar") %></a>

    <section class="top-bar-section">
      <ul class="right">
        <%- if current_user -%>
          <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, :class => "button hollow"%>
          <img class="profile-icon" src='<%= current_user.profile_photo %>' />
        <%- else -%>
          <%= link_to 'Sign Up', new_user_registration_path, :class => "button hollow" %>
          <%= link_to 'Sign In', new_user_session_path, :class => "button hollow" %>
        <%- end -%>
      </ul>
    </section>
  </nav>

  <%- flash.each do |key, value| -%>
    <div class="flash flash-<%= key %>">
      <%= value %>
    </div>
  <%- end -%>

  <%= yield %>

  <%= javascript_include_tag 'application' %>
  
  <%- if Rails.env.development? || Rails.env.test? -%> 
    <%= javascript_tag do %>
      window.railsEnv = '<%= Rails.env %>'
    <%- end -%>
  <%- end -%>
  <%= javascript_pack_tag 'application' %>

  <%= yield :extra_footer %>
</body>
</html>
